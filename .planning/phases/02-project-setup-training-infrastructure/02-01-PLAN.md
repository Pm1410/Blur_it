---
phase: 02
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - src/data/transforms.py
  - src/data/dataset.py
  - tests/test_dataset_loader.py
autonomous: true
requirements:
  - ARCH-04
must_haves:
  - requirements.txt specifying core dependencies
  - Transforms module src/data/transforms.py resizing to 128x128 and normalizing with ImageNet mean/std constants
  - PyTorch Dataset class NSFWDataset in src/data/dataset.py loading images and yielding (3, 128, 128) tensors with labels
  - Factory function get_dataloaders producing DataLoaders with batch shape (B, 3, 128, 128)
  - Unit tests verifying dataset loading, transform consistency, and DataLoader batch shapes
---

<objective>
Implement the PyTorch image dataset and DataLoader infrastructure for the NSFW classifier. Provide reusable transform pipelines for 128×128 normalization, the `NSFWDataset` class loading from folder structures, and `get_dataloaders` factory producing properly shaped batches for training and evaluation.
</objective>

<tasks>
<task id="02-01-01" type="auto">
<name>Implement transforms pipeline with documented normalization</name>
<files>
- src/data/transforms.py
</files>
<action>
1. Create `src/data/transforms.py`.
2. Define normalization constants: `NORM_MEAN = [0.485, 0.456, 0.406]` and `NORM_STD = [0.229, 0.224, 0.225]`.
3. Implement `get_transforms(image_size=(128, 128), is_training=False)` returning torchvision transform pipeline.
4. Export preprocessing specification metadata dictionary for downstream ONNX / JS web inference consumption.
</action>
<verify>
Run unit tests or check that `get_transforms()` converts PIL images to `(3, 128, 128)` torch float32 tensors.
</verify>
<acceptance_criteria>
Transforms pipeline resizes to 128×128, converts to tensor, and applies normalization.
</acceptance_criteria>
</task>

<task id="02-01-02" type="auto">
<name>Implement NSFWDataset and DataLoader factory</name>
<files>
- src/data/dataset.py
</files>
<action>
1. Create `src/data/dataset.py` implementing `NSFWDataset(Dataset)`.
2. Support loading from split directory (`data/processed/train`, etc.) with class subdirectories (`safe`, `nsfw`, `graphic`).
3. Load images safely using Pillow converting to RGB before applying transforms.
4. Implement `get_dataloaders(processed_dir, batch_size=32, num_workers=2)` returning `(train_loader, val_loader, test_loader)`.
</action>
<verify>
Run python snippet testing that `NSFWDataset` loads samples and DataLoader yields batches with shape `(batch_size, 3, 128, 128)`.
</verify>
<acceptance_criteria>
DataLoader successfully batches images into `(B, 3, 128, 128)` shape with integer class targets.
</acceptance_criteria>
</task>

<task id="02-01-03" type="auto">
<name>Implement unit tests for dataset loading and transforms</name>
<files>
- tests/test_dataset_loader.py
</files>
<action>
1. Create `tests/test_dataset_loader.py` covering:
   - Transform output shape `(3, 128, 128)` and dtype `torch.float32`.
   - `NSFWDataset` length and sample retrieval `(tensor, label)`.
   - `get_dataloaders` batch generation, batch shape `(B, 3, 128, 128)`, and target shape `(B,)`.
2. Run tests via pytest.
</action>
<verify>
Run `PYTHONPATH=. .venv/bin/pytest tests/test_dataset_loader.py -v` and assert all tests pass.
</verify>
<acceptance_criteria>
All dataset loader and transform tests pass with zero errors.
</acceptance_criteria>
</task>
</tasks>

<verification>
Execute pytest test suite:
`PYTHONPATH=. .venv/bin/pytest tests/test_dataset_loader.py -v`
Ensure:
1. `NSFWDataset` loads samples from disk.
2. Batches have shape `(B, 3, 128, 128)`.
3. Labels are integers in range `[0, 2]`.
</verification>

<success_criteria>
- `requirements.txt` present and dependencies verified.
- `NSFWDataset` and transforms implemented and tested.
- DataLoader batches verified to produce `(B, 3, 128, 128)` tensors.
</success_criteria>
