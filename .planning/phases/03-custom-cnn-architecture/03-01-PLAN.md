---
phase: 03
plan: 01
type: tracer
wave: 1
depends_on:
  - "02-01"
files_modified:
  - src/models/__init__.py
  - src/models/classifier.py
  - tests/test_model.py
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - ARCH-05
must_haves:
  - Custom CNN architecture with 4 Conv-BN-ReLU-Pool blocks in src/models/classifier.py
  - Global Average Pooling layer following the 4th conv block
  - Dense classification head (256 -> 64 -> 3) with Dropout p=0.5
  - Input tensor shape (B, 3, 128, 128) producing output shape (B, 3)
  - Total parameter count under 500,000 (< 2MB binary size)
  - Random initialization strictly enforced without pretrained weights
  - Unit tests verifying forward pass, parameter count budget, and random weight initialization
---

<objective>
Implement and validate the custom CNN architecture `NSFWClassifier` fulfilling requirements ARCH-01 through ARCH-05. Ensure strict random initialization, parameter budget under 500k, and 100% test coverage.
</objective>

<tasks>
<task id="03-01-01" type="auto">
<name>Implement NSFWClassifier architecture</name>
<files>
- src/models/__init__.py
- src/models/classifier.py
</files>
<action>
1. Define `ConvBlock` module with Conv2D(3x3, padding=1), BatchNorm2D, ReLU, and MaxPool2D(2, 2).
2. Define `NSFWClassifier` with 4 conv blocks (channels 3 -> 32 -> 64 -> 128 -> 256).
3. Add `nn.AdaptiveAvgPool2d((1, 1))` Global Average Pooling.
4. Add dense head: `Linear(256, 64) -> ReLU -> Dropout(0.5) -> Linear(64, 3)`.
5. Implement `_init_weights()` with Kaiming normal initialization.
</action>
<verify>
Run smoke test verifying model instantiates and accepts `(B, 3, 128, 128)` tensors.
</verify>
<acceptance_criteria>
`NSFWClassifier` model defined and outputs `(B, 3)` logits.
</acceptance_criteria>
</task>

<task id="03-01-02" type="auto">
<name>Validate parameter count and unit test suite</name>
<files>
- tests/test_model.py
</files>
<action>
1. Implement unit tests in `tests/test_model.py` covering:
   - Output shape `(B, 3)` on input `(B, 3, 128, 128)`
   - Parameter count < 500,000
   - Absence of NaN/Inf in logits
   - Validation that weights are randomly initialized
2. Run pytest suite.
</action>
<verify>
Run `PYTHONPATH=. .venv/bin/pytest tests/test_model.py -v` and assert all tests pass.
</verify>
<acceptance_criteria>
All model tests pass and parameter count is confirmed < 500k.
</acceptance_criteria>
</task>
</tasks>

<verification>
Execute `PYTHONPATH=. .venv/bin/pytest tests/test_model.py -v` and verify:
1. `NSFWClassifier` forward pass succeeds with dummy batch.
2. Parameter count is ~405k (< 500k budget).
3. Output shape is `(B, 3)`.
</verification>

<success_criteria>
- Custom CNN architecture implemented from scratch (ARCH-01, ARCH-02, ARCH-03, ARCH-04, ARCH-05).
- Model parameters < 500k and memory footprint < 2MB.
- All unit tests pass.
</success_criteria>
