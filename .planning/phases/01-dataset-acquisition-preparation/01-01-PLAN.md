---
phase: 01
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/__init__.py
  - src/data/__init__.py
  - src/data/download.py
  - data/dataset_license.json
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  - Python environment setup with dependencies (torch, torchvision, pillow, imagehash, scikit-learn, tqdm, requests) via uv
  - Automated downloader in src/data/download.py capable of fetching and organizing public permissive images into safe, nsfw, and graphic classes
  - License documentation file data/dataset_license.json specifying dataset sources and open license terms
  - Raw images organized into data/raw/{safe,nsfw,graphic}
---

<objective>
Bootstrap the Python project environment with required image processing and ML packages using `uv`, establish the `src/data` package structure, and implement an automated dataset downloader that acquires publicly licensed images across three classes (safe, nsfw, graphic) into `data/raw/` while recording verified license documentation.
</objective>

<tasks>
<task id="01-01-01" type="auto">
<name>Initialize Python environment and project configuration</name>
<files>
- pyproject.toml
- .gitignore
- src/__init__.py
- src/data/__init__.py
</files>
<action>
1. Create `pyproject.toml` with project metadata and dependencies: `torch`, `torchvision`, `pillow`, `imagehash`, `scikit-learn`, `numpy`, `tqdm`, `requests`, `pytest`.
2. Update `.gitignore` to ignore virtual environment (`.venv/`), raw data (`data/raw/`), processed data (`data/processed/`), cache files (`__pycache__/`, `.pytest_cache/`), and model weights (`*.pt`, `*.onnx`).
3. Initialize the virtual environment using `uv venv` and install project dependencies using `uv pip install -e .` or `uv pip install`.
4. Create package init files `src/__init__.py` and `src/data/__init__.py`.
</action>
<verify>
Run `.venv/bin/python -c "import PIL, torch, torchvision, imagehash, sklearn; print('All core dependencies imported successfully!')"`
</verify>
<acceptance_criteria>
Virtual environment exists and all core packages import without errors.
</acceptance_criteria>
</task>

<task id="01-01-02" type="auto">
<name>Implement automated public dataset acquisition script</name>
<files>
- src/data/download.py
- data/dataset_license.json
</files>
<action>
1. Author `src/data/download.py` with CLI options to download or assemble sample/full datasets across `safe`, `nsfw`, and `graphic` categories.
2. Ensure download script supports automated retrieval from verified public permissive datasets / mirrors with fallback test fixtures.
3. Save raw images into `data/raw/safe/`, `data/raw/nsfw/`, and `data/raw/graphic/`.
4. Generate and save `data/dataset_license.json` documenting the dataset source URL, license type (e.g. CC0, CC-BY, Open Access), and timestamp.
</action>
<verify>
Run `.venv/bin/python -m src.data.download --max-samples 100 --dry-run-check` and verify images are downloaded into `data/raw/{safe,nsfw,graphic}` and `data/dataset_license.json` is created.
</verify>
<acceptance_criteria>
`data/dataset_license.json` exists with verified license information and `data/raw/` contains images in `safe`, `nsfw`, and `graphic` subdirectories.
</acceptance_criteria>
</task>
</tasks>

<verification>
Execute `pytest` or standalone verification script checking that:
1. `.venv/bin/python` can run and load all modules.
2. `data/dataset_license.json` contains valid JSON with fields for source, license, and date.
3. Subdirectories `data/raw/safe`, `data/raw/nsfw`, and `data/raw/graphic` exist and contain image files.
</verification>

<success_criteria>
- Project dependencies installed in isolated virtual environment via `uv`.
- Publicly available dataset downloaded and separated into `safe`, `nsfw`, `graphic` classes under `data/raw/`.
- License details documented in `data/dataset_license.json` confirming permissible hackathon/demo use.
</success_criteria>
